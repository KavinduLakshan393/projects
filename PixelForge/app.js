// Base Configuration State
const state = {
    gridSize: 8,
    pixelSize: 45,
    currentColor: '#000000',
    currentTool: 'draw', // 'draw' or 'erase'
    isDrawing: false
};

// Initial Console Check to guarantee setup success
console.log("PixelForge Structure Initialized Successfully.");